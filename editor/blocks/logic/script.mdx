---
title: Bloco de Script
icon: code
---

O bloco "Script" permite que você execute código JavaScript.

<Info>Este bloco não permite criar um bloco visual personalizado</Info>

<Frame>
  <img src="/images/blocks/logic/code.png" width="600" alt="Bloco de código" />
</Frame>

<Info>
As variáveis no script não são analisadas, elas são avaliadas. Portanto, devem ser tratadas como se fossem variáveis JavaScript reais.

Você precisa escrever `console.log({{My variable}})` em vez de `console.log("{{My variable}}")`
</Info>

## Função `setVariable`

Se você deseja definir um valor de variável com JavaScript, o [bloco Set variable](./set-variable) é mais adequado para a maioria dos casos.

No entanto, se desejar definir variáveis com os blocos de script, você pode usar a função `setVariable` em seu script:

```js
if({{My variable}} === 'foo') {
  setVariable('My variable', 'bar')
} else {
  setVariable('My variable', 'other')
}
```

A função `setVariable` está disponível apenas em script executado no servidor, portanto, não funcionará se a opção `Executar no cliente?` estiver marcada.

## Exemplos

### Recarregar página

```js
window.location.reload()
```

### Redirecionar se uma variável tiver um valor específico

```js
if({{Category}} === 'qualified') {
  window.location.href = 'https://my-site.com'
}
```

Precisa fazer algo, mas não tem certeza de como fazer? [Junte-se ao servidor Discord](https://typebot.io/discord) e obtenha ajuda instantânea!
\```
